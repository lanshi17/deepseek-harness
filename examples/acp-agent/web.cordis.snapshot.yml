# Keyless replay counterpart to web.cordis.yml: the web stack and loopback
# fixture server stay real (the tool call re-executes the actual HTTP fetch and
# markdown rendering); only the model adapter is replaced by replay.
- id: base
  name: '@lanshi17/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - id: llm-deepseek
        name: '@lanshi17/dsh-llm-deepseek'
        disabled: true
      - insert:
          - id: web
            name: '@lanshi17/dsh-web'
          - id: web-fetch-http
            name: '@lanshi17/dsh-web-fetch-http'
          - id: web-fetch-fixture
            name: './web-fetch-fixture-server.mjs'
          - id: tool-web
            name: '@lanshi17/dsh-tool-web'
            config:
              search: false
          - id: llm-replay
            name: '@lanshi17/dsh-llm-replay'
            config:
              providers:
                - id: deepseek-official
                  name: DeepSeek
                  models:
                    - id: deepseek-v4-flash
                    - id: deepseek-v4-pro
