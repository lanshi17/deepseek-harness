# Test-only composition of the public opt-in provider and foreground tool.
# The owning e2e boots this tree but never invokes the model or Codex.
- id: fixture
  name: './fixture.ts'

- id: subagent
  name: '@lanshi17/dsh-subagent'

- id: subprocess
  name: '@lanshi17/dsh-subprocess-local'

- id: subagent-codex
  name: '@lanshi17/dsh-subagent-codex'

- id: tool-subagent-codex
  name: '@lanshi17/dsh-tool-subagent'
  config:
    provider: codex
    toolName: subagent_codex
    enableRunInBackground: false
    maxDepth: 'provider-managed'

- id: agent-spine
  name: '@lanshi17/dsh-agent-spine-demo'
  config:
    agents:
      - id: main
        provider: mock
        model: mock-delegate
        cwd: !!js process.cwd()
    persona: 'This composition test must not start a model turn.'
    workspaceContext: false

- id: persistence
  name: '@lanshi17/dsh-session-persistence-jsonl'
  config:
    root: './.sessions'

- id: checkpoint-policy
  name: '@lanshi17/dsh-session-checkpoint-policy'
