# Add the persisted goal domain and its model-facing tools to the real one-shot app.
- id: base
  name: '@lanshi17/cordis-plugin-include'
  config:
    path: ./cordis.yml
    patches:
      - insert:
          - id: goal
            name: '@lanshi17/dsh-goal'
          - id: tool-goal
            name: '@lanshi17/dsh-tool-goal'
